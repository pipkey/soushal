import {AllActionTypes, SidebarType} from "./store";

let initialState = {};

const sidebarReducer = (state: SidebarType = initialState, action: AllActionTypes) => {
    return state
};


export default sidebarReducer;