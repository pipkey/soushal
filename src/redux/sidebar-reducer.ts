import {SidebarType} from "./store";
type SidebarTypeActionType ={}
let initialState = {};

const sidebarReducer = (state: SidebarType = initialState, action: SidebarTypeActionType ) => {
    return state
};


export default sidebarReducer;